{
  "name": "dhmv-potree-next-viewer",
  "private": true,
  "version": "1.0.0",
  "description": "Minimal Potree Next viewer for the DHMV COPC",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "start": "npm run dev"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "license": "AGPL-3.0-only",
  "devDependencies": {
    "vite": "^8.2.1"
  },
  "dependencies": {
    "copc": "0.0.9",
    "laz-perf": "0.0.7"
  }
}
